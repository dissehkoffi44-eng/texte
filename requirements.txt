# Core pour skey (versions approximatives compatibles – testées fin 2025)
torch>=2.0.0
torchaudio>=2.0.0
streamlit>=1.30.0
# Très fortement recommandés (souvent nécessaires avec torch audio)
numpy
librosa>=0.10.0   # utile si tu veux ajouter des visualisations chroma plus tard
soundfile         # backend alternatif pour lire certains formats audio

# Optionnel mais utile pour la stabilité / performances
tqdm
pillow
